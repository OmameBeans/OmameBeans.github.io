import { Box } from "@mui/material";

export const HomePage = () => {
    return (
        <Box sx={{
            display: "grid",
            gridTemplateColumns: "20rem auto 20rem",
        }}>

        </Box>
    );
}